machine:
  xcode:
    version: "8.3"

checkout:
  post:
    - ./install.sh

dependencies:
  override:
    - gem install xcpretty
    - gem install fastlane
    - npm install shelljs@0.7.0
    - brew update
    - brew install ant
       
test:
  override:
    - ./build/pre-build
    - fastlane CI 
